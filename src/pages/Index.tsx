import MainLayout from "components/MainLayout";

const IndexPage: React.FC = () => {
	return (
		<MainLayout>
			<div className='h-screen flex items-center justify-center'>Gold Loto Admin Panel</div>
		</MainLayout>
	);
};

export default IndexPage;
